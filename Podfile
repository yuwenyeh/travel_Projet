# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'travel_project' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for travel_project
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'Alamofire', '~>4.5'
  pod 'Kingfisher', '~>4.0'
  pod 'SwiftyJSON'
end
